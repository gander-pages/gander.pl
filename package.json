{
  "name": "gander.pl",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "release-it": {
    "git": {
      "requireBranch": "master"
    }
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "build-ci": "node -r dotenv/config ./node_modules/.bin/vitepress build",
    "preview": "vitepress preview",
    "release": "release-it --only-version"
  },
  "devDependencies": {
    "@sentry/vite-plugin": "^2.16.0",
    "@types/bun": "^1.0.11",
    "@types/node": "^20.11.30",
    "release-it": "^17.1.1",
    "vitepress": "^1.0.1"
  }
}
